language: common-lisp

env:
  matrix:
    - LISP=sbcl
    - LISP=abcl
    - LISP=ccl
    - LISP=ecl

install:
  - echo $PWD
  - ls
  - bash run.sh

script:
  - cl --eval '(print "hello")'
  - cl --eval '(ql:quickload "alexandria")'
  - cl --eval '(uiop:quit 0)'

notifications:
  email: false
